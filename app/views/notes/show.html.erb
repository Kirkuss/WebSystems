<p id="notice"><%= notice %></p>


<div class="card" style="width: 18rem;">
	<% @note_attachments.each do |p| %>
  <%= image_tag p.avatar_url, :size => "300x250	", class: "card-img-top"%>
  <% @currentuser = User.find_by name: session[:user] %>
  <% if  @currentuser == @note.user %>
   <%= link_to "Edit Attachment", edit_note_attachment_path(p), class: "badge badge-pill badge-warning" %>
 <% end %>
 <% end %>
  <div class="card-body">
  	<p class="card-text">  <strong>User:</strong><%= @note.user.name %></p>
    <p class="card-text">  <strong>Text:</strong><%= @note.text %></p>    
  </div>
</div>


 
  <br>


<% @currentuser = User.find_by name: session[:user] %>
<% if @currentuser == @note.user %>
<%= link_to 'Edit', edit_note_path(@note) %> |
<% end %>
<%= link_to 'Back', notes_path %>


